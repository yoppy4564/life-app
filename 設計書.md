ライフプランアプリ 設計書
システム概要
本システムはユーザーが自身の収入・支出情報および資産情報を入力することで、将来の資産推移をシミュレーションできるライフプランアプリです。入力されたデータをもとに資産残高の将来予測を行い、結果をグラフで視覚的にわかりやすく表示する点が特徴です[1]。ユーザーは収支を記録・管理し、資産形成の見通しをグラフで確認できます。
本アプリの技術スタックは以下の通りです。
	•	フロントエンド：Next.js（React + Redux Toolkit）を使用し、SPA/SSRによる快適なUIを提供します。グラフ描画にはChart.jsやRechartsなどのライブラリを利用し[2]、収支データの可視化を実現します。
	•	バックエンド：FastAPI（Python）を採用し、RESTful APIとして機能要件（ログイン認証、データ取得・更新など）を提供します。非同期処理や高パフォーマンスが特徴で、ビジネスロジックとデータアクセスを担当します。
	•	データベース：PostgreSQLを使用し、ユーザー情報や資産項目、収支記録を永続化します。リレーショナルDBの特性を活かし、各種情報を正規化して管理します。
	•	外部連携：本システムでは特に外部のサービスとの連携はなく、全て自社システム内で完結します。
目的は、ユーザーが収支や資産の情報を一元管理し、自分のライフプラン（将来のお金の計画）を可視化して検証できるようにすることです。例えば、月々の収支を登録することで将来の貯蓄額がどのように推移するかをグラフで確認でき、将来の大きな支出（住宅購入や教育費など）に備えた計画立案に役立てることができます。
システム構成図
図: システム全体の構成図（クライアント・サーバ・DBの関係）
上図に本アプリのシステム構成を示します。クライアント（ユーザーのブラウザ）上で動作するNext.js製フロントエンドと、FastAPIによるバックエンドAPIサーバ、そしてPostgreSQLデータベースから構成されます。各コンポーネントの役割は以下の通りです。
	•	ブラウザ（Next.jsフロントエンド）：ユーザーのWebブラウザ上で動作するシングルページアプリケーションです。React + Next.jsにより実装され、必要に応じてサーバサイドレンダリング(SSR)も活用します。状態管理にはRedux Toolkitを使用し、ユーザーの入力データや取得データを一元管理します。フロントエンドはバックエンドAPIとHTTP(S)経由で通信し、JSON形式のデータを送受信します。
	•	FastAPIサーバ：Python製の軽量高速なWebフレームワークFastAPIで構築されたREST APIサーバです。ユーザー認証、収支データや資産項目データの登録・取得・更新処理など、アプリのビジネスロジックを担います。各APIエンドポイントではHTTPメソッド（GET, POST, PUT, DELETEなど）に応じて、データベースとのやりとりを行います。セキュリティ面では後述するJWTによる認証認可を実装しています。
	•	PostgreSQLデータベース：アプリケーションのデータを保存するリレーショナルデータベースです。ユーザーアカウント情報、資産項目、収支の履歴データを格納します。FastAPIサーバはデータアクセスにORM（例えばSQLAlchemy）も利用し、安全なクエリ発行とデータ永続化を行います。
クライアントとサーバはREST APIで通信しており、例えばユーザーがログインするときはブラウザからFastAPIの認証用APIにリクエストを送り、認証成功時にJWTトークンを受け取ります。また、収支データの登録・取得要求もブラウザからAPIを呼び出し、サーバ側でDBを操作して結果を返します。
※本システムでは外部サービスとの連携は無く、すべて自前のフロントエンド・バックエンド・DB内で処理が完結します。
機能一覧と画面遷移構成
本アプリに実装される主要機能と、それに対応する画面および遷移関係を示します。
	•	ユーザー管理機能（認証）ユーザーの新規登録、ログイン、ログアウト、アカウント情報編集といった機能です。最初にユーザーはログイン画面からメールアドレス・パスワードでログインします（未登録の場合はサインアップ画面へ）。ログイン成功後、ユーザーにはJWTトークンが発行され、以降の認証が必要なAPI呼び出しに利用されます。ログイン後はメイン画面（ダッシュボード）へ遷移します。また、画面右上のアカウント編集ボタンからプロフィール情報の変更やパスワード変更が可能です（任意機能）。
	•	収支登録機能ユーザーが日々または月次の収入・支出の情報を記録する機能です。ダッシュボード画面から「収支登録」ボタン（または「＋」ボタン）を押下すると収支入力フォーム画面が表示されます。ユーザーは日付、金額、種別（収入 or 支出）、メモ等を入力し、登録を行います。登録された収支データはデータベースに保存され、ユーザーごとに管理されます。収支入力画面から保存後はダッシュボードに戻り、最新の収支情報に基づいたグラフが更新されます。
	•	将来予測グラフ表示機能ダッシュボード画面にて、ユーザーの現在の資産残高や今後の収支計画に基づいた将来資産残高の予測グラフを表示します。収支登録されたデータや資産項目の初期値等をもとに、一定の計算ロジック（例えば毎月の収支を積み上げ、必要なら年利等を加味）で将来の推移を算出します。グラフは年間の資産推移をラインチャート等で表示し、ユーザーが視覚的に将来の貯蓄額を把握できるようにします。グラフは動的に更新され、収支データの追加・編集に応じてリアルタイムに変化します。詳細なグラフ表示方法については後述します。
	•	資産項目編集機能資産項目とはユーザーごとに管理する資産の種類や口座のことです（例：預金口座、現金、投資信託など）。ログイン後、画面上部のアカウントメニューから「資産項目編集」画面に遷移できます。この画面ではユーザーが保有する資産項目の一覧を表示し、新規追加・名称変更・削除が行えます。例えば「普通預金」「株式投資」などの項目をユーザーが追加し、それぞれの初期残高や属性を設定できます。資産項目は将来予測グラフの内訳や収支記録時の対象資産選択に利用されます（収支記録の際にどの資産に影響するか選択することで、各資産毎の残高推移も管理可能）。資産項目編集画面から保存後はダッシュボードに戻ります。
画面遷移の概要：1. （未ログイン時）ユーザーはログイン画面または新規登録画面にアクセスする。2. ログイン成功後、ダッシュボード画面へ遷移し、将来資産グラフや収支一覧を表示する。3. ダッシュボードから各機能へ遷移: 収支登録ボタンで収支入力画面、資産編集ボタンで資産項目編集画面、ログアウトボタンでログアウト処理を実行しログイン画面へ戻る。4. 収支入力や資産編集の完了後はいずれもダッシュボードへ戻り、変更内容が反映された最新の情報を表示する。
※画面上部のナビゲーション（ヘッダー）にはダッシュボード、収支入力、資産編集など主要ページへのリンクが配置され、ユーザーは自由に行き来できます。ログアウトもヘッダーから可能です。
API仕様書
本節ではバックエンドFastAPIが提供する主なAPIエンドポイントについて、その仕様をまとめます。基本的にRESTfulな設計に従い、HTTPメソッドとエンドポイントURIで操作を識別します。データはJSON形式でやり取りされ、保護が必要なAPIはJWT認証によりアクセス制限されます。
認証関連API（認証が必要ないエンドポイント）- POST /api/auth/register – ユーザー新規登録: ユーザーアカウントを作成します。リクエストボディにemail、password、name等を含みます。成功時に201 Createdと共にユーザーIDやプロフィール情報を返します（または自動ログインさせトークン発行）。- POST /api/auth/login – ログイン: メールアドレスとパスワードで認証を行い、成功時にJWTトークンを発行します。リクエストボディにemail、passwordを含み、認証成功時はレスポンスでaccess_token（JWT）とトークンの有効期限等を返します。以降、ユーザーはこのトークンをHTTPヘッダー（Authorization: Bearer トークン）に添えて認証が必要なAPIを利用します。
ユーザー情報API- GET /api/user/me – 自分のユーザー情報取得: 現在認証中のユーザー自身のプロフィール情報を取得します。JWT認証が必要です。レスポンスにはユーザーID、名前、メールアドレス等の情報（パスワードハッシュは含まない）が含まれます。- PUT /api/user/me – ユーザー情報更新: 認証中ユーザーのプロフィールを編集します。例えば表示名の変更やパスワード変更（この場合は現パスワード確認と新パスワードハッシュ化処理）が可能です。リクエストボディに変更フィールドを含み、成功時200 OKを返します（必要に応じて更新後情報を返す）。
資産項目API- GET /api/assets – 資産項目一覧取得: ログインユーザーの資産項目リストを取得します。各資産項目にはid、name（名称）、initial_balance（初期残高）等のフィールドが含まれます。JWT認証が必要です。- POST /api/assets – 資産項目新規作成: 新しい資産項目を追加します。リクエストボディにnameやinitial_balance等を含みます。追加された資産項目のIDを含む情報を返します。※ユーザーIDは認証情報から取得し、指定する必要はありません（自動的に現在のユーザーに紐付け）。- PUT /api/assets/{asset_id} – 資産項目編集: 既存の資産項目（名称や初期残高等）を更新します。URLパラメータで資産IDを指定し、リクエストボディに変更内容を含めます。成功時に200 OKを返し、更新後のデータを返すこともあります。- DELETE /api/assets/{asset_id} – 資産項目削除: 指定した資産項目を削除します。削除により関連する収支記録への影響が出る場合は、事前に警告するか関連レコードの処理（例えば該当資産の収支を削除または別資産へ振替）を行います。成功時204 No Contentを返します。
収支記録API- GET /api/transactions – 収支記録一覧取得: 認証ユーザーの収入・支出記録の一覧を取得します。クエリパラメータで期間指定（例: 開始日・終了日や年度）やフィルタ（収入のみ/支出のみ、特定資産項目のみなど）が可能です。レスポンスは収支記録の配列で、各要素にid、date、amount、type（収入=income or 支出=expense）、asset_id（紐づく資産項目）等を含みます。データ量が多い場合はページネーション対応も検討します。- POST /api/transactions – 収支記録追加: 新たな収入または支出の記録を登録します。リクエストボディにdate（日付）、amount（金額）、type（種別）、asset_id（対象資産項目）、description（メモ任意）等を含めます。金額は収入の場合正の値、支出の場合負の値または別途typeで判断します。成功時201 Createdと登録内容（生成されたid含む）を返します。登録後、このデータは将来予測計算にも反映されます。- PUT /api/transactions/{id} – 収支記録編集: 既存の収支記録を更新します。URLで記録IDを指定し、変更したいフィールド（例：金額やメモ）をボディに含めて送信します。成功時200を返し、更新後のデータを返す場合もあります。- DELETE /api/transactions/{id} – 収支記録削除: 指定した収支記録を削除します。削除後は将来予測にも即座に反映され、グラフ表示から当該レコードの影響が消えます。成功時204 No Contentを返します。
将来予測関連API（オプション）- GET /api/projection – 将来資産予測データ取得: サーバ側で収支データと資産項目を基に予測計算を行い、グラフ描画用のデータポイントを返すエンドポイントです。例えば現在年から一定年数先まで、各年末時点の予測資産残高リストを返します。レスポンスは[{year: YYYY, total_asset: amount}, ...]のような形式を想定します。クライアント側でこのデータを受け取りグラフを描画します。※予測計算はクライアント側で実装する場合、本APIは不要です。その場合、フロント側で/api/assetsや/api/transactionsから取得したデータを用いて計算します。
以上が主要なAPI仕様です。すべての保護されたエンドポイント（ユーザー情報、資産項目、収支関連）はJWTトークンによる認可が必要です。トークンが無効または期限切れの場合、APIはHTTP 401エラーを返し再ログインを促します。またAPIは必要に応じてバリデーションエラー時に400 Bad Requestなど適切なステータスコードとエラーメッセージを返すよう設計します。
データベースER図とテーブル設計
本節では、本アプリで使用するデータベース（PostgreSQL）のエンティティとテーブル構造について示します。主要なテーブルは「ユーザー」「資産項目」「収支記録」の3つで、下図にER（エンティティ関連）図を示します。
図: データベースのER図（ユーザー、資産項目、収支記録）
上記ER図は、ユーザーと資産項目、収支記録のテーブルおよびそれらのリレーション関係を表しています。各ユーザーは複数の資産項目および収支記録を持ちうる（一対多の関係）構造です。また収支記録は一つの資産項目に紐づく（一対多）想定です。次に各テーブルの詳細設計を示します。
	•	ユーザー（Users）テーブル: ユーザーアカウント情報を保持します。
	•	id (PK): ユーザーを識別する主キー(ID)。数値の自動採番。
	•	name: ユーザー名。表示用の名前。
	•	email: メールアドレス。ユーザーのログインIDとしても使用し、一意制約を付与します。
	•	password_hash: パスワードのハッシュ値。安全のため平文ではなくハッシュ化（例：Bcrypt）して保存します。
	•	created_at: アカウント登録日時のタイムスタンプ。
	•	updated_at: アカウント情報更新日時。
	•	資産項目（AssetItems）テーブル: ユーザーごとの資産の項目（口座や資産カテゴリ）を管理します。
	•	id (PK): 資産項目を識別するID。数値の主キー。
	•	user_id (FK): 所有者であるユーザーのID。Usersテーブルの外部キー。
	•	name: 資産項目の名称。例：「普通預金」「現金」「投資信託」などユーザーが任意に設定。
	•	initial_balance: 資産項目の初期残高。シミュレーション開始時点でのこの資産の金額。
	•	category: 資産のカテゴリ種別（任意フィールド、例：預貯金/有価証券など分類したい場合）。
	•	created_at: 資産項目作成日時。
	•	収支記録（Transactions）テーブル: 個々の収入・支出の記録を保存します。
	•	id (PK): 収支記録ID。主キー。
	•	user_id (FK): 記録の所有ユーザーID。Usersテーブルを参照。
	•	asset_id (FK): 関連する資産項目ID。どの資産に対する収支かを示す。AssetItemsテーブルを参照。例えば給与収入なら「預金口座」資産に紐付け、家賃支出なら「預金口座」を減らす等。
	•	date: 収支発生日。日時または日付。
	•	amount: 金額。正の値は収入、負の値は支出を表すか、別途typeで区別。通貨単位は円。
	•	type: 収支種別（文字列またはENUM）。income（収入）またはexpense（支出）など。金額符号と併せて利用。
	•	description (またはnote): メモや記述。任意で、収支の内容説明を保存。
	•	created_at: この記録の登録日時。
	•	updated_at: 更新日時。
各テーブル間の参照関係としては、AssetItems.user_idとTransactions.user_idがUsersテーブルのidを参照する外部キーであり、Transactions.asset_idがAssetItemsテーブルのidを参照します。このことで、ユーザー削除時に関連する資産や収支をまとめて削除したり（ON DELETE CASCADE 等の設定検討）、あるいは一貫性を保つことが可能です。
テーブル設計上の考慮事項:- パフォーマンスのため、収支記録テーブルにインデックスを張る（特にuser_id、dateなどで複合インデックス）ことで、ユーザーごとの期間検索を高速化します。- 金額計算はアプリ側で行いますが、必要ならビューやストアドプロシージャで年間の集計を出すなども検討できます。今回はシンプルにアプリケーション層で集計・計算します。- パスワードはハッシュ化保存し、認証時に比較を行います。またメールはユニーク制約し、同一メールで複数登録できないようにします。
グラフ表示方法（サンプル）
将来予測グラフは、ユーザーの収支データおよび資産情報を基に計算された資産残高の時系列推移を可視化するものです。グラフ描画にはJavaScriptのチャートライブラリ（例：RechartsやChart.jsなど）が利用できます[2]。フロントエンド（Next.js）側で、取得したデータを加工してグラフコンポーネントに渡し描画します。グラフの種類は折れ線グラフ（ラインチャート）が適していますが、長期推移を見せるためX軸を年度（または年月）、Y軸を資産額としたラインチャートを採用します。将来推移の計算は以下のようなロジックで行います。
	•	各月または各年について、初期資産残高にその期間の収入合計を加算し、支出合計を減算します。これを次の期間への繰越残高とします。例えば年次シミュレーションでは、「前年末残高 + 当年収入総額 - 当年支出総額 = 当年末残高」とします。必要に応じて年利や投資利回り等を考慮する場合は、期間ごとに残高へ利息を乗じます。
	•	上記の計算をシミュレーション期間（例えば現在から20年後まで）繰り返し、各年末（または各月末）の予測残高を算出します。
	•	算出結果のデータ系列（年と残高のペア一覧）をグラフライブラリに渡し、折れ線グラフを描画します。グラフには将来年数に沿った滑らかな線で残高推移を示し、重要な転機（残高がマイナスになる、目標額に達する年など）が一目で分かるようになります。必要に応じて現在時点を縦線で示す、目標額を水平線で示すなどの工夫もできます。
図: 資産残高の将来推移グラフ（サンプル）
上図はサンプルの将来資産推移グラフです。横軸が年度、縦軸が資産残高（円）で、初期100万円から始まり毎年一定額の貯蓄を続けた場合の残高推移を例示しています。グラフは年々資産が増加している様子を示しており、このようにユーザーの入力した収支計画に基づく資産の増減を可視化します。実際のアプリでは、このグラフはダッシュボード上に表示され、ユーザーが収支データを更新するたびに即座に再計算・再描画されます。またマウスオーバーやタップで各年の具体的な数値（ツールチップ表示）を確認できるインタラクティブなグラフとなります。
グラフ表示の実装にあたっては、例えばRechartsを使用する場合、<LineChart>コンポーネントにデータ配列を渡し、X軸・Y軸やツールチップ等の子コンポーネントを配置して作成します。これにより直感的かつレスポンシブなグラフ描画が可能です。チャートライブラリを使うことで複雑なSVG操作を行わずに済み、開発効率と表示品質が向上します。
セキュリティ・認証の概要
本アプリではユーザー認証とセキュリティ対策としてJWT（JSON Web Token）による認証方式を採用しています。ユーザーがログインするとサーバ側で署名付きのJWTアクセストークンが発行され、クライアントはそれをローカルストレージやHTTPOnlyクッキーに保存します。以降、認証が必要なAPIリクエスト（ユーザー情報取得やデータ編集など）の際には、このトークンをAuthorizationヘッダーに付与して送信します。サーバ側では受け取ったJWTを検証し、正当なユーザーからのリクエストかを確認します。このJWT認証により、ユーザーの操作が常に検証され安全に行われます[3]。
JWTにはユーザーIDやトークン有効期限などのクレームが含まれ、適切な有効期限（例：1時間）を設定しています。期限切れトークンはサーバで拒否されるため、ユーザーは再ログインが必要です。さらにリフレッシュトークンの仕組みを導入し、長期ログインを実現することも検討できます（ただし今回は簡易化のためアクセストークンのみで対応）。
その他のセキュリティ対策として以下を考慮しています。
	•	パスワードの安全管理: ユーザーのパスワードは安全なアルゴリズムでハッシュ化して保存し、生のパスワードは保持しません。ログイン時にはハッシュ照合で認証します。
	•	入力バリデーションとエラー処理: FastAPIの機能（Pydanticなど）を活用し、API受信時に入力内容を厳格にチェックします。不正なデータや想定外の値はエラーとして弾き、不正入力によるバグやセキュリティホールを防ぎます。
	•	認可とアクセス制御: 各APIはJWTによるユーザー識別に基づき、自分のデータのみにアクセス可能とします。他人のユーザーIDを指定した操作ができないよう、サーバ側で認可チェックを行います。例えば/api/transactions?user_id=2のようなリクエストは無視し、認証トークンのユーザーIDに紐づくデータのみに限定します。
	•	通信の暗号化: アプリはHTTPS上で動作し、ネットワーク上での通信内容（JWTや個人情報、収支データなど）はTLSにより暗号化されます。これにより盗聴や改ざんのリスクを低減します。
	•	その他一般的な対策: APIへの過度なリクエストに対するレートリミットを検討し（ブルートフォース攻撃対策[4]）、またXSS/CSRF対策としてJWTはHTTPOnlyクッキーで保存するか、SPAの場合は適切なCSRFトークンと組み合わせるなどを考慮します。
以上のように、本システムでは認証・認可機能をJWTで実装し、その他セキュリティベストプラクティスを組み合わせることで、安全なライフプラン管理を提供します。ユーザーのデータは適切に保護され、安心してアプリを利用できるよう設計されています。[3]

[1] 人生設計に役立つおすすめ無料アプリ3選＆シミュレーションサイト - マネコミ！〜お金のギモンを解決する情報コミュニティ〜
https://manekomi.tmn-anshin.co.jp/kakei/17592346
[2] 〖個人開発〗ライフプランアプリを開発しました #JavaScript - Qiita
https://qiita.com/ryome/items/c2446bfd075d558db34f
[3] [4] GitHub - georgekhananaev/PyNextStack: Next.Js, FastAPI a Full-Stack User Management System designed to facilitate comprehensive user management, including registration, authentication, and profile management. At its core, it leverages FastAPI for the backend operations, ensuring secure and efficient data handling. The frontend is built with Next.js and Material-UI (MUI)
https://github.com/georgekhananaev/PyNextStack
